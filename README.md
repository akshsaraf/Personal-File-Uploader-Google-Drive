# Auto Cloud Backup Tool

Auto backup tool with versioning, Google Drive upload, and email notifier.

## Setup
- Edit `config/config.json`
- Place `credentials.json` from Google Cloud Console
- Run: `python main.py`